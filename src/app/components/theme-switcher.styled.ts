import ReactSwitch from 'react-switch';
import { styled } from '@mui/material/styles';
import { MoonIcon, SunIcon } from '../icons';

export const SwitchThemeStyled = styled(ReactSwitch)({});

export const MoonIconStyles = styled(MoonIcon)({
  marginLeft: 3,
  marginTop: 3,
  width: 23,
  height: 23,
});

export const SunIconStyles = styled(SunIcon)({
  marginLeft: 3,
  marginTop: 3,
  width: 23,
  height: 23,
});
